language: cpp
script: rake
compiler:
  - clang
  - gcc

before_install:
  - sudo add-apt-repository ppa:boost-latest/ppa -y
  - sudo apt-get update -qq
  - sudo apt-get install -y libboost1.55-dev libboost1.55-system-dev libboost1.55-filesystem-dev || apt-cache search libboost
